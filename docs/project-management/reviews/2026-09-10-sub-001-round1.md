 # 獨立審查報告：20260909 字幕輸出（SUB-001）

 - 審查對象：`/Users/nycu/Documents/離線字幕工廠/subtitle-outputs/` 三份 SRT、`subtitle-work/generate-20260909-subtitles.mjs`、四份 `english-*.tsv`
 - 輸入：`/Users/nycu/Downloads/20260909.mp4.edited.srt`、`/Users/nycu/Downloads/rule.txt`
 - 審查輪次：round1
 - 審查時間：2026-09-10 08:08（Asia/Taipei）
 - 上下文來源：獨立只讀上下文；未沿用主要開發代理的評價或結論。

 ## 審查方法與總體證據

 - 獨立 Node.js 解析器逐 cue 解析輸入與三份 SRT：輸入及三份輸出均為 1,129 段。
 - 三份輸出均逐段通過序號與時間碼對齊；每份均為有效 `HH:MM:SS,mmm --> HH:MM:SS,mmm` 格式，均有最終換行。
 - 四份 TSV 行數為 `300 + 300 + 300 + 229 = 1,129`；ID 範圍分別為 1–300、301–600、601–900、901–1129，ID 唯一、無缺漏、無額外 cue。
 - 中文修正版相對輸入改變 99 個 cue；變更集中於腳本 `correctionById` 及全域專名／錯字正規化，不見整篇翻譯重做。
 - 英文獨立版與 TSV 有 15 個字串差異，全部是腳本第 115–116 行將 `Evercam`／`Lti` 正規化為 `EVERCAM`／`LTI`；這與 `rule.txt` 專名全大寫要求一致。

 ## 1. 需求完整性

 - 判定：通過。
 - 證據：腳本第 121–124 行確實輸出中文修正版、中英合併版、獨立英文版；三份實物均存在且為 1,129 cue。輸入規則第 8–11 行要求的專名與繁體中文範圍，輸出可見 `E3`、`EVERCAM`、`LTI`、`AI`、`YouTube`、`Turnitin` 等正規化結果；本輪使用者明確要求「只做明顯錯別字／ASR 誤植修正」，因此未將 `rule.txt` 第 2–6 行的全篇口語刪除、標點重排等較廣泛編輯誤套到輸出。
 - 中文修正例證：`20260909.mp4.edited.corrected.zh-TW.srt:42` 將「單一入口往登進去」修為「單一入口網登入」；`:1474` 將 `evercam` 修為 `EVERCAM`；`:3982` 將 `e3` 修為 `E3`。

 ## 2. 邏輯正確性

 - 判定：通過。
 - 證據：腳本第 8–13 行從同一輸入 cue 保留 ID／時間碼；第 89–109 行載入 TSV、拒絕無效／空白／重複 ID 並檢查 1,129 筆完整性；第 111–118 行由同一 cue 序列產出三版，故三版的 cue 順序與時間碼不會分岔。
 - 例外處理符合本次資料：第 75 行只對 cue 12、26 折疊來源中的重複辨識行；實際輸出仍保留 cue 12／26 的單一中文行，沒有造成 cue 遺失。
 - 英文大小寫差異可解釋：TSV 原始資料仍含 `Evercam`，輸出生成時由第 115 行統一為 `EVERCAM`，不是翻譯內容錯位。

 ## 3. 邊界情況

 - 判定：通過（就本輪已指定資料而言）。
 - 證據：已檢查 UTF-8／無 BOM、LF、最終換行、多行來源 cue、連續 ID、四段 TSV 邊界、空白分隔、所有輸出時間碼格式；三份輸出均無格式錯誤。中文修正版未發現 `，。！？、；：` 等規則指定不保留的標點；輸出未發現小寫 `evercam`、`lti`、`e3`。
 - 未覆蓋但不構成本輪輸出缺陷的情況：未改寫腳本測試來源 ID 跳號、重複 ID、非 1,129 cue 或含 Tab 的翻譯資料；腳本第 104–108 行對本次固定輸入有明確防護，但不是泛化 SRT 驗證器。

 ## 4. 輸出資料品質

 - 判定：通過。
 - 證據：三份 SRT 的 cue 對齊、中文／英文內容分版及雙語合併關係均一致；中英版每 cue 由中文行後接英文行，沒有漏行或跨 cue 串接。四份 TSV 可完整重建英文版，僅有預期的 15 個專名大小寫正規化差異。
 - 修正範圍符合要求：99 個中文 cue 的差異可由第 16–70 行的明確字串修正與第 73–86 行正規化規則解釋；未見套用整篇口語刪除或翻譯重寫。
 - 注意：`rule.txt` 第 8 行寫「專有名詞必須全大寫」，但同一行列出的 `DALL-E`、`GPT-4` 等本身含小寫／連字符語義；本報告依其實際指定拼法及輸出內容判斷，不將合法品牌拼法誤判為缺陷。

 ## 5. 測試覆蓋

 - 判定：部分通過。
 - 實際執行證據：
   - `npm run project:preflight -- --type=full`：成功列出固定核心與 full 路由文件。
   - `node --check subtitle-work/generate-20260909-subtitles.mjs`（2026-09-10 08:08）：exit 0。
   - 獨立解析器：`source 1129`、三份 `count 1129 alignment OK`、`translations 1129 unique 1129 missing [] extra []`、`zh changed 99`、格式 `formatBad 0`。
   - TSV 行數與首尾 ID：300／1–300、300／301–600、300／601–900、229／901–1129。
 - 未覆蓋：未實際啟動播放器或以原影片聲音／畫面逐句人工驗證；未建立針對 99 個中文修正的期望值回歸測試。故測試足以證明本次資料與生成結果一致，但不足以證明 ASR 語意在原影片中全部正確。

 ## 6. 實際運行結果

 - 判定：部分通過。
 - 證據：已對現有實物執行全量解析、對齊、ID／時間碼／格式／專名及 TSV 重建核對，全部通過；腳本語法檢查通過。未直接執行生成腳本，因其第 121–124 行會覆寫三份交付輸出，而本輪限制只允許寫入本報告檔；因此本輪的「實際運行」證據是現有產物的可解析性與與腳本邏輯的全量一致性核對，不宣稱完成播放器／原影片 smoke test。

 ## 問題清單

 - 無需阻擋交付的問題。
 - [P2，非阻擋] 四份 TSV 中 15 個 `Evercam`／`Lti` 拼法與輸出不同（例如 `english-301-600.tsv:69` 對應 cue 369），但輸出由腳本第 115–116 行正規化為規則要求的 `EVERCAM`／`LTI`；若要求 TSV 也作為最終可讀交付物，應另行同步 TSV，否則目前僅是中間資料與輸出資料的已知差異。
 - [P2，驗收風險] 未以原影片及播放器驗證 ASR 語意、英文閱讀速度與實際換行；這是未覆蓋風險，不是由目前結構證據確認的輸出錯誤。

 ## 六面向總結

 | 面向 | 判定 |
 |---|---|
 | 需求完整性 | 通過 |
 | 邏輯正確性 | 通過 |
 | 邊界情況 | 通過（指定資料範圍） |
 | 輸出資料品質 | 通過 |
 | 測試覆蓋 | 部分通過 |
 | 實際運行結果 | 部分通過 |

 ## 綜合判定

 - 結論：有條件通過。
 - 完整單句結論：**SUB-001 已完整產生三份 1,129 段字幕，序號、時間碼、三版 cue 對齊、中文修正範圍、專名大小寫與 SRT 格式均通過全量核對；但本輪未直接執行會覆寫產物的生成腳本、播放器或原影片語音／畫面驗收，且 TSV 與最終英文輸出存在 15 個預期大小寫正規化差異，因此判定為有條件通過，不宣稱完成最終媒體品質驗收。**
 - 阻擋問題：無。
 - 剩餘風險：未做原影片語音／畫面逐句驗證；未做播放器閱讀性 smoke；TSV 若需直接交付，仍需決定是否同步專名大小寫；腳本的數量檢查固定為 1,129，非泛化輸入驗證器。
 - 建議：若要把判定提升為無條件通過，應在可安全寫入隔離輸出目錄的副本上重跑生成腳本，並以播放器／原影片抽樣驗收；不要直接覆寫目前三份交付檔。

 ## 審查代理聲明

本審查代理除建立本報告外，僅執行讀取、指令執行與驗證，未修改任何其他專案檔案；未重做翻譯。
